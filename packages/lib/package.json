{
  "name": "@siguiente/lib",
  "module": ".output/index.js",
  "types": "./index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --bundle --minify --outdir .output --target bun --format esm index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@noble/curves": "^1.9.2"
  }
}
